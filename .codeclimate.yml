version: "2"
checks:
  argument-count:
    config:
      threshold: 16
  file-lines:
    config:
      threshold: 600
  method-count:
    config:
      threshold: 50
  method-lines:
    config:
      threshold: 50
exclude_patterns:
- test/
- docs/
- build/
- dist/
